function Input(props) {
  return (
    <input
      {...props}
      style={{
        width: "100%",
        padding: "10px",
        marginBottom: "10px",
      }}
    />
  );
}

export default Input;